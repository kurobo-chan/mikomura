import * as React from "react";
import Nav from "./nav";

const Header = () => {
  return (
    <React.Fragment>
      <header className="header">
        <Nav />
      </header>
    </React.Fragment>
  );
};
export default Header;
